<Window x:Class="WpfClient.Views.AssignmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClient.Views"
        mc:Ignorable="d"
        Title="AssignmentWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0">
            
        </ListBox>
        <StackPanel Grid.Column="1" Orientation="Vertical">
            <ListBox>
                
            </ListBox>
            <Button Content="Create Assignment" x:Name="btn_create" Click="btn_create_Click"></Button>
            <Button Content="Update Assignment" x:Name="btn_update" Click="btn_update_Click"></Button>
            <Button Content="Delete Assignment" x:Name="btn_delete" Click="btn_delete_Click"></Button>
        </StackPanel>
    </Grid>
</Window>
